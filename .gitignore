# Secrets — never commit credentials of any kind
kaggle.json
access_token
*.token
.env
.kaggle/

# Python
__pycache__/
*.pyc
.venv/

# Generated Kaggle upload payload (regenerate via kaggle/stage_payload.py)
kaggle/payload/
kaggle/upload/
kaggle/_tree/

# Local scratch: single-component push folders + Kaggle output fetches
pt*/
ptout_fetch.log

# Experiment artifacts land in the repo ONLY via deliberate commits of logs/
# (raw scratch outputs stay out)
scratch/
tmp/
